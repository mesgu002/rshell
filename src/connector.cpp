#include "../header/connector.h"

using namespace std;

connector :: connector (base* lhs, base* rhs)
{
	left = lhs;
	right = rhs;
}

connector :: connector ()
{
	left = NULL;
	right = NULL:
}

#endif
